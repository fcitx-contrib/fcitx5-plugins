{
  "name": "fcitx5-plugins",
  "type": "module",
  "scripts": {
    "lint": "eslint tests --ignore-pattern 'tests/rime/*'",
    "lint:fix": "eslint --fix tests --ignore-pattern 'tests/rime/*'",
    "check": "tsc --noEmit",
    "test": "playwright test",
    "test:chromium": "playwright test --project chromium",
    "test:firefox": "playwright test --project firefox",
    "test:webkit": "playwright test --project webkit"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^7.0.0",
    "@playwright/test": "^1.57.0",
    "@types/node": "^24.10.1",
    "eslint": "^10.0.0",
    "serve": "^14.2.5",
    "typescript": "^5.9.3"
  }
}
